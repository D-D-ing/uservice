type Query {
  # The API Version
  version: String!

  # Get user with ID
  user(id: ID!): User

  # Get user by name
  userByName(name: String!): [User]

  # Get user by email
  userByEmail(email:String!): User
}
